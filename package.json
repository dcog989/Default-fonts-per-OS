{
	"name": "default-fonts-per-os",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"check": "biome check .",
		"clean": "rm -rf .context node_modules .vitest bun.lock",
		"dev": "bun server.js",
		"format": "biome format .",
		"format:fix": "biome format --write .",
		"lint": "biome lint .",
		"lint:fix": "biome lint --write .",
		"update": "bun update --latest"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.7"
	}
}
